---
const { title, description, url, image, author } = Astro.props

let subtitle = 'Houston Robotics Group'
---

<!-- general meta -->
<meta name="Houston Robotics Group" content={`${title} - ${subtitle}`} />
<meta name="Weekly Robotics Meetup Every Saturday" content={description} />
<meta name="Houston Robotics Group" content={author} />

<!-- open graph -->
<meta property="og:title" content={`${title} - ${subtitle}`} />
<meta property="og:description" content={description} />
<meta property="og:type" content="website" />
<meta property="og:url" content={url} />
<meta property="og:image" content={Astro.site ? `${Astro.site}${image}` : image} />

<!-- twitter card -->

<!-- page title -->
<title>{title} - {subtitle}</title>
